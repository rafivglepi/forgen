[package]
name = "example-plugin"
version = "0.1.0"
edition = "2021"
description = "Example Forgen dylib plugin — demonstrates the plugin API without any ra_ap_* dependencies."

[lib]
name = "example_plugin"
crate-type = ["cdylib"]

[dependencies]
forgen-api = { path = "../api" }
